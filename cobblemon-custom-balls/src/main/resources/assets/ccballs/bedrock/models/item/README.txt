Aca van los modelos 3D (formato Bedrock/GeckoLib .geo.json) que Cobblemon usa para
la ball cuando se lanza/aparece en el mundo (la que se ve volar y abrirse).
Nombra los archivos igual que el identifier de la ball, ej: sikko_ball.geo.json
